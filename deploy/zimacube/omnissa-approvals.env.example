# Omnissa Access Approvals — ZimaCube env file
# Lives at /media/ZIMARAID/omnissa-approvals/omnissa-approvals.env, chmod 600.

# ── Omnissa Access service client (approvals API) ────────────────────────────
OMNISSA_BOOTSTRAP_URL=dean.us1.wss.workspaceone.com
OMNISSA_BOOTSTRAP_CLIENT_ID=ApprovalService
OMNISSA_BOOTSTRAP_CLIENT_SECRET=

# ── First-run local admin (created only when the user table is empty) ────────
OMNISSA_BOOTSTRAP_ADMIN_USERNAME=admin
OMNISSA_BOOTSTRAP_ADMIN_PASSWORD=
OMNISSA_BOOTSTRAP_ADMIN_EMAIL=admin@approvals.flaming.ws

# ── Admin OAuth2 login (Omnissa Access as OIDC IdP) ──────────────────────────
OMNISSA_ADMIN_OAUTH_CLIENT_ID=ApprovalAdmin
OMNISSA_ADMIN_OAUTH_CLIENT_SECRET=
OMNISSA_ADMIN_OAUTH_REDIRECT_URI=https://approvals.flaming.ws/login/oauth2/code/omnissa
# Must match the issuer in /.well-known/openid-configuration (it is /SAAS/auth, not /acs)
OMNISSA_ADMIN_OAUTH_ISSUER_URI=https://dean.us1.wss.workspaceone.com/SAAS/auth
# Set true after confirming OAuth2 login works to auto-disable the consent screen
OMNISSA_ADMIN_OAUTH_DISABLE_CONSENT=false

# ── SMTP (optional) ───────────────────────────────────────────────────────────
SPRING_MAIL_HOST=
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
